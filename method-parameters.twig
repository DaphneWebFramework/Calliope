{%for item in method.arguments%}{{item.getType()|raw}} {%if item.byReference%}&{%endif%}{%if item.isVariadic%}...{%endif%}${{item.name}}{%if item.default is not null%} = {{item.default|raw}}{%endif%}{%if not loop.last%}, {%endif%}{%endfor%}